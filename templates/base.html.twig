<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>Вопросы с нечеткой логикой</title>
        <style>
            body{margin-left: 25px}
            * {font: 15px 'Tahoma'}
            h1{font-weight: 700; font-size: 30px}
            h3{font-weight: 500; font-size: 18px}
            ol, ul{padding-left: 5px; margin: 0 0 15px 15px}
            ul{margin-left: 0}
            ul li{list-style: none; padding: 0}
            a, button{border-radius: 4px; display: inline-flex; padding: 8px 12px; background: #518eec; border: none;
            cursor: pointer; color: #fff; text-decoration: none}
            div.success, div.fail{margin: 0 0 25px 0}
            div.success{color: green}
            div.fail{color: red}
        </style>
    </head>
    <body>
        {% block body %}{% endblock %}
    </body>
</html>